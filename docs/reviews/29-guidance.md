# Change 29: conditional prompt guidance and explicit saved-copy upgrades

Replaced broad sensory expansion, generic dialogue polish and compulsory surface variation with function-specific, meaning-preserving guidance. Added the managed workflow skill, original schema-valid setup illustration and conditional voice calibration instructions. Existing progression/counterplay guidance remains; managed routing supersedes only legacy prose, review and completion paths.

The author-facing upgrade command reads a named shipped baseline, previews a three-way merge, retains author conflicts, and requires the preview digest to apply. Omitting a baseline preserves unknown current guidance and exposes incoming text. Package installation never overwrites the saved project copy. Historical guidance is migration data, not active model context.

Review: syntax, strict types and 127 local tests pass. Actual command dispatch tests check no mutation on load/preview, preserved author additions, explicit conflicts, safe baseline IDs, no paid authorization and disabled-by-default migration settings. Review found false conflicts from a separating blank line; section equality now normalizes only platform newlines and trailing blank separators, retaining selected raw section text and the byte-exact migration backup. Example schemas are validated, not claimed to be calibrated voice data.

No live provider or reader study was run. Local Node remains 22.16.0. Updated prompts need behavior evaluation in the supported Pi host before literary efficacy claims.
